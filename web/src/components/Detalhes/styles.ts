import styled from 'styled-components';

export const Container = styled.div`
    width:65rem;
    display:flex;
    flex-direction:column;
    max-width:100%;
    background:var(--color-background);
    position:fixed;
    box-shadow:2px 2px 10px var(--color-label);
    /* zIndex:999; */
    left:30%;
    top:30%;
    padding:3rem 2rem 0 3rem; 
    margin:0 auto;

 `;